# ── Web ─────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ── HTTP client (Last.fm, ListenBrainz, MusicBrainz, every AI backend) ──
httpx==0.28.1

# ── Track matching and download ─────────────────────────
ytmusicapi==1.9.0
# A floor, not a pin. YouTube breaks extractors every few months and a fix
# usually ships within days, so the image also upgrades yt-dlp at boot unless
# YTDLP_AUTO_UPDATE=false — see docker/entrypoint.sh.
yt-dlp>=2026.7.4
mutagen==1.47.0

# ── Scan schedule ───────────────────────────────────────
APScheduler==3.11.0

# ── Testing (not installed in the image) ────────────────
# pytest==8.3.4
